---
type: project_learning_checkpoint
status: active
project: AI-Career-Transition
learning_stage: Phase 1-A closure - LLM minimum mechanism
checkpoint_role: rolling_learning_pointer
current_focus: 完成固定 logits 与随机种子的采样参数受控实验，再推进有/无证据对照、最小文本到文本推理链复核和位置编码边界诊断。
durable_stage_artifact: pending
previous_stage_artifact: 02_Projects/AI-Career-Transition/LLM训练机制系统学习文档.md
single_pass_recoverable: false
summary: 作为固定的当前阶段学习恢复入口，记录正在进行的学习焦点、薄弱项、下一步和持久阶段产物。
sources:
  - 2026-07-21 第二阶段主动学习对话
  - 2026-07-24 第二阶段续测与滚动检查点结构决策
  - 2026-07-25 Adam/AdamW 闭卷检查、训练主链复述与最小实践代码审查
  - 2026-07-26 TinyCausalLM forward/loss 运行评估
  - 2026-08-01 TinyCausalLM 单步训练运行、有效 token 加权、AMP 与 checkpoint 主动学习续测
  - 2026-08-04 TinyCausalLM 单 batch 过拟合、确定性 eval、checkpoint 轨迹恢复与 Phase 1 训练机制阶段收尾
  - 2026-08-06 用户复核 Phase 1-A 原始任务清单并确认恢复到 closure 阶段
  - 2026-08-06 Phase 1-A 架构、Transformer block、证据边界与 scaled attention 数学机制主动诊断
  - 02_Projects/AI-Career-Transition/AI职业转型项目总览.md
  - 02_Projects/AI-Career-Transition/LLM最小推理机制系统学习文档.md
  - 02_Projects/AI-Career-Transition/多模态AI职业转型学习方案.md
scope: encoder/decoder 架构对比、Transformer block 边界、证据与拒答机制、scaled dot-product attention 实现、采样实验和证据对照实验。
risks:
  - 本文是滚动恢复指针，不是完整学习文档，也不作为项目总体事实源。
  - 训练机制阶段的运行结果来自用户报告并经过静态代码审查，未由代理独立复跑，不得外推为生产训练或跨设备恢复验证。
  - Phase 1-A 已诊断的理论主题目前只达到对话诊断意义上的 working；位置编码边界与受控实验仍未完成，不得外推为完整覆盖、独立实现或故障定位能力。
  - scaled dot-product attention 独立实现由用户明确标记为 waived_by_scope；这不是实现完成证据。
  - Phase 1-B 只作为排队中的下一阶段；Phase 1-A closure 门禁未满足前不得把评测练习作为当前主线。
  - 每次切换阶段前，必须先把阶段成果沉淀到持久系统学习文档，避免历史随滚动替换丢失。
  - 未完成独立 recoverability verification，保持 single_pass_recoverable 为 false。
updated_at: 2026-08-06
---

# 1 当前阶段学习检查点

## 1.1 检查点用途

本文件是项目内固定的滚动学习恢复入口。项目总览长期只链接本路径；阶段切换时先生成持久的阶段系统学习文档，再把本文更新到下一阶段。

本文不是五份 current 文档组，不承担项目总体事实源，也不保存完整历史。历史由阶段系统学习文档保存。

滚动规则：

```text
阶段中：更新当前焦点、评测结果、薄弱项和下一步
阶段结束：生成持久系统学习文档
完成校验：在项目总览登记持久文档
阶段切换：把本检查点改为下一阶段
```

状态解释：

```text
已验证：能够主动解释主要机制或完成检查题。
部分闭合：理解主干，但在边界题中仍需提示或纠正。
待学习：当前回答表明心智模型尚未建立。
未评测：尚未进入该覆盖区。
```

## 1.2 当前阶段结论

LLM 训练机制子主线已经在当前学习范围内完成，持久结果见 [[02_Projects/AI-Career-Transition/LLM训练机制系统学习文档]]。但重新对照 [[02_Projects/AI-Career-Transition/多模态AI职业转型学习方案]] 的 `1.5.1 阶段 A` 后，原始理论与实践任务尚未全部闭合。

因此，本滚动检查点恢复为 `Phase 1-A closure - LLM minimum mechanism`。Phase 1-B 的评测框架已经具备材料基础，但暂不作为当前主阶段。

2026-08-06 主动学习已使架构边界、multi-head attention、residual、LayerNorm、FFN 和证据/拒答边界达到 working；scaled dot-product attention 的公式、shape、mask、softmax 维度与缩放方差也达到 working。位置编码尚未单独诊断。用户决定跳过 attention 独立实现，该项记录为 `waived_by_scope`，不计为实现完成。

## 1.3 上一阶段完成证据

- 已闭卷解释 next-token loss、SFT masking、梯度流、Adam/AdamW、混合精度顺序、global step 与 checkpoint 主干。
- `/home/jichao/test/llm_practice.py` 已取得单步 finite loss、有限非零梯度和参数变化证据。
- 用户运行单 batch 过拟合路径，报告确定性 eval loss 为 `0.0916125476360321`，有效 token accuracy 为 `3/3`。
- 用户运行 CPU 内存 checkpoint 恢复路径，报告 loss、gradient norm、LM Head delta 和最终参数最大差值均为 0。
- 本轮静态代码审查确认比较对象、参数 clone、model/optimizer 加载和 RNG 恢复时点与上述实验目标一致。

证据边界：运行输出由用户提供，代理未独立复跑；当前 checkpoint 未持久化 `global_step`，也未验证磁盘序列化、scheduler、GradScaler、CUDA RNG 或 DataLoader/Sampler 状态。因此本结论只关闭最小学习主线，不声明生产训练闭环或独立 recoverability verification。

## 1.4 已完成且不重复展开的内容

- token、embedding、causal mask、逐层 Q/K/V、logits、decoding 与 KV cache 已达到对话诊断意义上的 working。
- next-token loss、SFT masking、梯度流、Adam/AdamW、混合精度、global step 与 checkpoint 主干已完成主动学习检查。
- TinyCausalLM 的最小训练、确定性 eval 和 CPU 内存 checkpoint 轨迹恢复证据继续有效。
- encoder/decoder 架构对比、multi-head/residual/LayerNorm/FFN 职责和证据/拒答边界已通过本轮主动诊断。
- scaled dot-product attention 数学机制、shape、mask、softmax 维度与缩放目的已通过诊断；独立实现未执行。

这些内容只在新问题暴露相关缺口时复查，不用重复低价值训练实验阻塞 closure。

## 1.5 当前未闭合覆盖区

### 1.5.1 理论诊断（部分 working）

1. **架构边界**：encoder、decoder、encoder-decoder 与 decoder-only 的输入、attention 可见性和任务差异。
2. **Transformer block**：multi-head attention、residual、normalization 与 FFN 已达到 working；位置编码及具体顺序的架构差异仍待诊断。
3. **证据边界**：幻觉、unsupported claim、引用证据、拒答和不确定性表达之间的关系。

### 1.5.2 受控实践（当前）

1. scaled dot-product attention 数学机制已达到 working；独立实现和参考测试由用户决定 `waived_by_scope`，不得记录为已实现。
2. **当前项**：固定输入与随机种子，改变 temperature、top-p、最大输出长度和上下文，记录可观察变化并解释原因。
3. **待执行**：构造有证据与无证据对照，验证回答、引用、拒答和不确定性行为。
4. **待复核**：现有 TinyCausalLM 是否真正闭合 `文本 → token → embedding → causal attention → logits → decoding → 文本`；未覆盖部分应显式补齐或记录边界。

## 1.6 恢复顺序

1. 阅读本文“当前阶段结论”和“当前未闭合覆盖区”。
2. 阅读 [[02_Projects/AI-Career-Transition/多模态AI职业转型学习方案]] 的 `1.5.1 阶段 A：LLM 最小机制闭环`。
3. 已达到 working 的理论主题除非暴露新缺口，不重复追问；保留位置编码边界诊断。
4. 依次完成采样参数实验、证据对照实验和最小文本到文本推理链复核，再补齐位置编码边界。
5. 对照完成门禁复核证据；满足后再把本文滚动到 Phase 1-B。

本恢复顺序只提高人工恢复清晰度；`single_pass_recoverable` 继续保持为 `false`。

## 1.7 Phase 1-A closure 完成门禁

- [x] 能闭卷比较 encoder、decoder、encoder-decoder 与 decoder-only 的数据流和 attention 边界。
- [ ] 能解释 multi-head attention、位置编码、residual、normalization 与 FFN 的作用和实现差异边界；除位置编码外的本轮已问组件达到 working。
- [x] 能区分 unsupported claim、fabricated evidence、证据引用、安全拒答和证据不足 abstain，并处理一个缺证据反例。
- [x] 能闭卷推导 scaled dot-product attention 的 shape、mask、softmax 维度和 `sqrt(D)` 缩放；独立实现由用户决定 `waived_by_scope`，不计为实现完成。
- [ ] 已完成固定输入的采样参数受控实验，并能解释观察结果。
- [ ] 已完成有证据/无证据对照实验，并记录回答、引用或拒答行为。
- [ ] 已复核最小文本到文本推理链，明确哪些节点已有运行证据、哪些仅为理论理解。

在上述条件满足前，不把 Phase 1-B 设为当前主阶段；也不进入 VLM benchmark、微调或架构扩展。
