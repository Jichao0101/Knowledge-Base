---
type: project_plan
status: planned
project: AI-Career-Transition
summary: 结合知识库已有 DMS、端侧部署和 Agent 工程项目，按“能力诊断—LLM 机制—评测练习—VLM 实践—系统加固”推进的 12 个月学习与作品建设方案。
sources:
  - 2026-09-05 用户说明有模型训练经验但缺少硬件基础，Part4 阅读抽象；确认 P02A-01 为经 AI 整理优化的个人学习笔记，并要求更新阶段、目标与检查点
  - 2026-07-17 用户确认的学习目标、项目背景与隐私边界
  - 2026-08-10 用户修正 AI Career 主动学习方法：主动学习不得被误用为纯考试，知识大面积缺失时应先给教学骨架再诊断吸收
  - 2026-08-20 用户确认 Phase 1-C 范围关闭，并将模型工程认知设为 Phase 2-A 进入 OMS VLM 适配前的桥接阶段
  - 02_Projects/AI-Career-Transition/AI职业转型项目总览.md
  - 02_Projects/DMS/03_Model_Training/model_training_overview_current.md
  - 02_Projects/DMS/08_EyeStatus/eyestatus_overview_current.md
  - 02_Projects/agent-trajectory/agent_trajectory_overview_current.md
scope: 默认每周投入 10 至 12 小时；以多模态应用和可评测 AI 系统为主线，按阶段证据滚动调整。
risks:
  - 同时推进多模态模型、Agent 和系统工程容易广而不深，任一时段只允许一个主项目进入实现态。
  - 在尚不能解释 LLM/VLM 输入输出、失败机制和评分边界时过早采集 benchmark，容易形成数量化但不可解释的伪基线。
  - 远程模型 API 可降低原型成本，但不能替代数据治理、评测、可靠性、成本和端云降级能力。
  - AI 辅助完成的设计与实现不能直接视为独立掌握证据，必须补充解释、修改、测试或故障定位验证。
updated_at: 2026-09-05
---

# 1 多模态 AI 职业转型学习方案

## 1.1 项目背景与隐私边界

本方案以知识库中已经存在的工程项目为背景，而不是从完全抽象的能力画像出发：

- DMS Model Training 与 EyeStatus 项目提供视觉感知、数据处理、模型训练、量化部署、后处理和端侧约束场景。
- Agent 原型、workflow、agent-trajectory 与知识库治理项目提供工具调用、状态、轨迹、证据链和异步处理场景。
- 已有项目中的 AI 辅助设计和实现只能作为实践材料，不能直接视为独立掌握证据。

上述内容只描述已授权项目的技术事实和可复用资产，不记录学历、雇主、任职时长，也不声明个人在这些项目中的任职关系或职责范围。默认每周投入约 10 至 12 小时；若投入下降，优先延长阶段而不是压缩验证。

路线目标不是补齐基础模型预训练研究履历，而是以 DMS/OMS 和 Agent 工程项目为迁移载体，形成“多模态模型 + 评测 + Agent runtime + ML Systems”的可验证工程闭环。

## 1.2 一年后的目标能力

完成本方案后，应能独立完成以下闭环：

1. 解释文本从 tokenization、embedding、causal attention、logits 到 decoding 的 LLM 推理数据流，以及上下文、采样和证据约束如何影响输出。
2. 解释 Transformer、ViT 和 VLM 的核心结构，以及图像 token、projector 与语言模型之间的数据流。
3. 完成一个开源 VLM 的推理、领域数据构造、参数高效微调和分组评测。
4. 设计端侧感知、结构化状态、本地小模型和云端大模型之间的路由。
5. 构建具备结构化工具调用、状态持久化、重试、幂等、降级和审计证据的 Agent。
6. 建立真实 benchmark，报告任务成功率、幻觉/无依据结论、延迟、成本和人工复核时间，并说明评分边界与一致性。
7. 对 AI 辅助生成的关键模块进行独立解释、修改、测试和故障定位。
8. 形成两个能够用于求职的旗舰项目，并能用证据说明独立贡献边界、失败模式和系统取舍。

## 1.3 优先级与范围控制

### 1.3.1 三层优先级

| 优先级 | 内容 | 范围规则 |
|---|---|---|
| Must | LLM/Transformer/VLM 数据流、评测基本原理、结构化 Agent、推理与可观测性 | 未通过阶段门禁不得跳过 |
| Should | 参数高效微调、端云路由、性能与成本优化 | 只在基线和 benchmark 稳定后进入 |
| Later | 基础模型预训练、复杂多 Agent 编排、大规模训练平台 | 不纳入前 9 个月主线 |

### 1.3.2 执行约束

- 同一时段只维护一个主项目；第二个旗舰项目在第一个项目形成稳定 benchmark 后再启动。
- 每两周只承诺一个可验证交付物，阅读和课程必须服务于该交付物。
- 优先使用最小可行模型和最小数据集验证链路，再决定是否微调、扩模型或引入新框架。
- 新增学习主题时必须同时删减或推迟一个低优先级主题。

### 1.3.3 每周节奏

默认每周投入 10 至 12 小时，但学习早期和项目阶段采用不同配比。

Phase 0 与 Phase 1 前半段按“教学骨架—主动重建—最小实现—复盘”分配：

- 2 至 3 小时：建立本周主题的最小教学骨架，先看定义、数据流、关键变量、边界和一个可运行或可观察例子。
- 2 小时：主动重建，闭卷画图、费曼复述、对比相邻概念和回答 1 至 3 个诊断题。
- 4 小时：最小实现、受控实验、基线运行或故障定位。
- 1 至 2 小时：根据诊断结果补最小缺口，更新阶段学习文档、代码复核和下周范围冻结。

形成 benchmark 草案后按“构建—评测—复盘”分配：

- 5 小时：当前阶段的模型或系统实现。
- 3 小时：benchmark、测试、失败样本与性能测量。
- 2 小时：补齐直接阻塞交付物的理论与源码阅读。
- 1 至 2 小时：闭卷复述、代码复核和下周范围冻结。

若每周只有 6 小时，学习早期保留核心机制、最小实现和复盘，推迟评测集扩充；形成 benchmark 草案后再保留实现、评测和复盘。始终暂停扩展阅读与第二技术栈。

### 1.3.4 主动学习使用规则

主动学习用于暴露和修补心智模型，不等同于复习，也不等同于连续考试。每个主题先判断知识覆盖状态，再选择教学与诊断比例：

| 覆盖状态 | 学习顺序 | 不应做什么 |
|---|---|---|
| 大面积缺失 | 先给最小教学骨架，再做主动重建和检查题 | 不要求凭空回答未知材料 |
| 局部混淆 | 用短诊断题定位最小缺口，再做微型补课 | 不把所有概念重新讲一遍 |
| 主干已通 | 用边界题、反例题和迁移题检查是否能应用 | 不停留在术语复述 |
| 准备交付 | 用实践、失败样本和可复现证据验证能力 | 不用对话正确替代运行证据 |

一次学习闭环默认按以下顺序进行：

```text
确认阶段边界
→ 建立阶段学习文档骨架与学习记录
→ 按骨架教学
→ 主动重建
→ 缺口诊断
→ 微型补课
→ 迁移或实践题
→ 诊断结果写入学习记录
→ 按缺口受控更新学习文档
→ 实践证据写入实践记录
→ 更新滚动检查点
```

教学骨架必须先回答五件事：

1. 这个概念或系统解决什么问题。
2. 输入、输出和中间状态是什么。
3. 关键模块如何连接，哪些变量控制结果。
4. 哪些边界条件会让结论失效。
5. 后续实践如何观察它是否真的工作。

诊断题只用于决定补什么、练什么和是否进入实践。除阶段关闭或明确复测外，不把诊断题包装成正式测试；对话中的 `working` 只表示当前问答范围内的可用理解，不替代代码、基线、样本、指标或失败分析。

阶段学习文档只承载稳定教学内容；个人回答、诊断结论、掌握状态、环境快照和当前下一步写入学习记录；真实配置、命令、原始输出、指标与失败分析写入实践记录。详细职责、更新边界和模板见 [[02_Projects/AI-Career-Transition/00_规划/学习文档维护规范]]。

## 1.4 Phase 0：能力诊断与范围冻结（第 1 至 2 周）

### 1.4.1 目标

建立真实能力基线，判断 LLM、Transformer、VLM、Agent 和评测分别处于什么理解层级，防止把“看过、让 AI 写过、运行过”误判为“独立掌握”。本阶段不建立正式 benchmark。

### 1.4.2 任务

- 不使用 AI，解释 token、embedding、next-token prediction、causal mask、logits、decoding、上下文窗口和 KV cache；不会的内容如实标记，不要求一次答对。
- 不使用 AI，解释 self-attention、cross-attention、ViT patch embedding 和 VLM projector，区分“能复述术语”和“能画出数据流”。
- 限时阅读或补全一段最小 LLM/Transformer 推理代码，用于判断代码理解、实现和调试起点；不以完成复杂模型为目标。
- 对现有 `agent_from_scratch` 做一次轻量代码审计，列出工具调用、状态、错误处理、测试和可观测性缺口，不在本阶段展开 Agent 重构。
- 从已有 DMS/OMS 项目中选择一个可公开描述、数据使用合规的场景，定义端侧快路径与云端语义路径的输入输出。
- 候选题目最多保留两个，只允许一个进入实现；另一个只保留一页 problem statement。
- 记录 3 至 5 个用于暴露认知缺口的诊断样例；这些样例不计为 benchmark，不用于宣称模型或方案效果。

### 1.4.3 退出条件

- 形成能力矩阵：`可独立实现 / 能解释但需参考 / 仅使用过 / 未学习`。
- 形成 LLM/VLM 前置缺口清单，并按“阻塞后续实践 / 可边做边补 / 暂不需要”分级。
- 形成一个主项目的一页式 problem statement，写清问题、非目标、数据边界、指标和两周内首个交付物。
- 能画出最小 LLM 推理数据流；若不能，则 Phase 1 首个交付物必须先补齐该闭环。
- 未完成基线测评前，不进入大规模框架学习。

## 1.5 Phase 1：LLM、Transformer、VLM 与评测基础（第 3 至 12 周）

### 1.5.1 阶段 A：LLM 最小机制闭环（第 3 至 5 周）

- tokenizer、token、embedding、causal language modeling 和 next-token prediction。
- attention、multi-head attention、位置编码、残差、归一化和 FFN。
- encoder、decoder、decoder-only 模型的数据流差异。
- logits、greedy decoding、temperature、top-k/top-p 和停止条件。
- 上下文窗口、KV cache、prompt 输入与模型参数知识的边界。
- 幻觉、无依据结论、引用证据、拒答和不确定性表达的基本机制。

实践任务：

1. 使用可观察的极小模型或最小代码，打通 `文本 → token → embedding → causal attention → logits → decoding → 文本`。
2. 独立实现并测试 scaled dot-product attention；根据 Phase 0 结果决定是否继续实现完整最小 Transformer block。
3. 固定输入，改变 temperature、top-p、最大输出长度和上下文，记录输出变化并解释原因。
4. 构造一个有证据和无证据的对照实验，观察模型何时应回答、引用或拒答。

### 1.5.2 阶段 B：评测基本功与练习集（第 6 至 8 周）

学习内容：

- 区分确定性任务、开放生成任务、检索/证据任务和工具执行任务。
- 区分 exact match、规则评分、语义评分、模型评分和人工评分的适用边界。
- 理解 gold answer、acceptable answer、evidence requirement、abstain、失败标签和评分一致性。
- 理解数据泄漏、样本重复、指标漂移以及“为了当前模型定制评分规则”的风险。

实践任务：

1. 从已冻结的 problem statement 中选择一个范围足够小、结果可以复核的子任务。
2. 构造 3 至 5 个评测练习样例，逐个写清输入、预期行为、可接受答案、证据要求、拒答条件和失败标签。
3. 由本人隔天重复评分一次；若同一样例评分不一致，先修订任务或 rubric，不扩充数据。
4. 跑一个最简单基线，目标是验证任务和评分是否可用，不宣称模型能力优劣。

本阶段产物称为“评测练习集”或 `seed cases`，不得称为真实 benchmark。

### 1.5.3 阶段 C：VLM 基线与 benchmark 草案（第 9 至 12 周）

学习与实践任务：

1. 学习 ViT、视觉 token、图像 encoder、projector 和语言模型连接方式。
2. 理解图像/视频采样、上下文长度、KV cache 和推理成本。
3. 运行一个开源小型 VLM，完成图像问答与批量推理。
4. 从公开、合成或明确授权的数据构造小型座舱多模态任务集；若使用外部数据，先按知识库规则建立来源记录。
5. 先完成 zero-shot 与 few-shot 基线；只有错误分类表明微调可能解决主要误差时，才进行一次参数高效微调。
6. 除总体指标外，至少按输入质量、场景难度和任务类型做分组评测。

只有同时满足以下条件，才把练习集升级为 benchmark 草案：

- 能解释模型的输入输出和至少三类主要失败模式。
- 每个 case 都有可复核的期望行为或证据边界。
- 评分规则经过至少一次重复评分检查。
- 已存在一个最简单基线和可复现命令。

### 1.5.4 交付物

- LLM 最小推理数据流说明、受控实验记录和必要的最小实现测试。
- 评测练习集、评分一致性记录和最简单基线。
- VLM 数据说明、基线配置、评测脚本和实验报告；若执行微调，再补充训练配置。
- 至少一份失败样本分类，不以平均准确率作为唯一结论。

### 1.5.5 退出条件

- 能独立解释 LLM 从文本输入到生成输出的数据流，并说明采样、上下文和证据约束的作用。
- 能独立画出并解释 VLM 从图像输入到文本输出的数据流。
- 能定位一次数据、显存、输出格式或评测错误，而不是只依赖 AI 修复。
- benchmark 草案包含 15 至 30 个可复现 case；先保证评分可复核和失败类型覆盖，不为满足数量下限填充低价值样本。
- 只有经过重复评分或第二评分者检查、主要失败标签基本稳定后，才冻结首版评分规则。
- 若执行微调，能说明其相对最简单基线的增益、代价和适用边界；若不执行，记录不微调的证据。

## 1.6 Phase 2：多模态座舱端云原型（第 4 至 6 个月）

### 1.6.1 目标

把已有 DMS/OMS 感知、状态和后处理项目升级为“低层感知 + 结构化状态 + 大模型语义理解”的分层系统。

### 1.6.2 Phase 2-A：模型工程认知与 OMS 适配准备

当前在 Phase 2-A 内前置“GPU 基础桥接”子阶段。依据 2026-09-05 用户自述，已有训练经验但缺少硬件基础；先补计算与访存、执行与存储模型，结合向量加法和矩阵乘法例子，再返回 Part4、单卡资源观测和后续并行决策。以机制解释和例子迁移验收，不按固定学习次数或视频完成度验收。

P02A-01 定位为经 AI 整理优化的个人学习笔记；当前进度与能力证据以学习记录和 [[02_Projects/AI-Career-Transition/20_学习记录/当前阶段学习检查点]] 为准。原直接从单卡 SFT/显存开始的顺序保留于学习记录第 1.7 节，以下目标作为 GPU 桥接之后的主线：

1. 重建从授权数据、chat template、visual/text tokens、label mask 到 forward/backward、optimizer、checkpoint 的单卡 VLM SFT 主链。
2. 分解参数、梯度、optimizer state、activation 和 buffer 的训练显存，理解 LoRA/QLoRA、checkpointing、precision 与 FlashAttention 分别改变什么。
3. 用容量、吞吐和通信解释 DDP、FSDP/ZeRO、TP、PP、CP，不以框架名替代选型理由。
4. 用 HBM/SRAM、tiling、fusion 和 recomputation 建立算子 IO 心智模型，并说明 visual tokens 对 attention 的影响。
5. 为假设性的 OMS VLM 数据适配设计 profiler、质量评测和停止条件。

当前只要求认知与考核闭环，不强制完成 SFT、多机训练或 CUDA/Triton kernel。OMS 任务合同、合法数据、稳定基线、错误分类和资源预算明确后，再决定是否执行最小 LoRA/QLoRA。

阶段学习文档：[[02_Projects/AI-Career-Transition/10_学习文档/P02A-01_VLM模型工程认知_学习文档]]。

### 1.6.3 推荐任务

从以下方向选择一个，不并行铺开：

- 根据短视频、驾驶状态和车辆信号解释驾驶员行为。
- 摄像头事件、CAN 状态和历史上下文联合判断座舱需求。
- 乘员场景理解与受约束的主动服务建议。
- 视觉事件到语言解释再到受约束工具动作的完整链路。

### 1.6.4 架构要求

```text
端侧轻量感知或事件抽取
    -> 结构化状态与证据
    -> 本地模型 / 云端 VLM 路由
    -> 语义结论与不确定性
    -> Agent 权限和状态门禁
    -> 提醒、解释或模拟车控动作
```

### 1.6.5 必做对照

对以下输入策略进行质量、成本和延迟比较：

- 原始图像或抽帧图像。
- ROI 图像。
- 端侧感知结构化结果。
- 图像与结构化状态组合。
- 本地摘要后再调用云端模型。

云端 API 可以作为主路径之一，但必须实现：

- timeout 和 retry 上限。
- 弱网/断网降级。
- 敏感数据最小化。
- 数据保留周期和脱敏策略。
- 请求和模型版本记录。
- 成本与延迟统计。
- 无法获得可靠结论时的 abstain 行为。

### 1.6.6 退出条件

- benchmark 扩展到至少 50 个真实、合成或可复现 case，并包含边界与拒答样本。
- 报告端侧路径、云端路径和混合路径的质量、延迟、成本及失败模式。
- 路由策略必须相对最简单基线带来可解释收益；否则回退到更简单架构。
- 不把“Demo 能回答”当作任务完成。

## 1.7 Phase 3：Agent Systems 与 ML Systems 加固（第 7 至 9 个月）

### 1.7.1 Agent 能力

- 使用原生结构化工具调用和参数 schema，替代正则解析工具标签。
- 显式定义任务状态、工具状态、失败状态和终止状态。
- 实现 timeout、有限重试、幂等键、断点恢复和审计日志。
- 区分原始事实、模型推断、不确定性和最终动作。
- 对高风险动作增加权限、确认或 dry-run 门禁。

### 1.7.2 ML Systems 能力

- 模型服务、请求队列、批处理、缓存和并发控制。
- 端云模型路由、fallback 和 circuit breaker。
- tracing、metrics、结构化日志和版本追踪。
- 模型、prompt、tool 和数据版本的回归测试。
- GPU/NPU/CPU 资源、吞吐、p50/p95 延迟和成本分析。

### 1.7.3 已有项目复用

- 用 `agent_from_scratch` 验证最小 Agent runtime 原理，但不直接把原型视为最终作品。
- 用 `agent-trajectory` 和知识库治理能力承载轨迹、事件、证据链和失败分类，但必须单独验证可靠性。
- 从已授权的 DMS 问题分析场景中构造任务 benchmark，评测 evidence completeness 和 unsupported claim；不得带入未公开数据或个人身份信息。

### 1.7.4 退出条件

- Agent benchmark 至少包含 50 个任务，并保留失败、高风险和拒答任务。
- 能输出 task success rate、tool-call success rate、unsupported claim rate、人工复核时间、延迟和成本。
- 至少完成一次模型或 prompt 版本升级的回归对比。
- 关键状态在进程重启或单步失败后可恢复；无法恢复的状态必须被检测并明确终止。

## 1.8 Phase 4：作品闭环与求职迁移（第 10 至 12 个月）

### 1.8.1 旗舰项目一：多模态座舱端云 AI 系统

至少包含：

- 明确业务问题和非目标。
- 端侧感知或事件抽取。
- VLM/LLM 语义理解。
- 端云路由和降级。
- 数据、模型和 prompt 版本。
- benchmark、分组指标和失败分析。
- 延迟、成本、隐私与可靠性边界。

### 1.8.2 旗舰项目二：可评测的领域 Agent 系统

至少包含：

- 真实领域任务集。
- 结构化工具调用和状态持久化。
- evidence chain。
- timeout、retry、幂等和恢复。
- 无 Agent、简单 Agent、加固 Agent 的对照。
- 任务成功率、无依据结论、成本和人工复核时间。

### 1.8.3 求职材料

- 简历只保留能明确说明个人职责、指标和验证证据的内容。
- 项目描述使用“问题—约束—决策—结果—失败边界”，避免罗列框架。
- 准备一次 30 分钟系统设计讲解和一次不使用 AI 的核心模块编码演示。
- 目标岗位优先匹配多模态 AI、VLM 应用、Agent Systems、ML Systems 和端云协同。

## 1.9 阶段门禁与验证规则

阶段切换以退出条件为准，月份只作为计划上限。连续两次复盘未达到退出条件时，必须缩小任务或降低模型复杂度，不得直接把未完成项滚入下一阶段。

六个强制门禁：

1. 不能解释 LLM 的最小推理数据流，不进入 VLM benchmark 采集。
2. 没有可复核任务定义和评分规则，不把诊断样例或评测练习集称为 benchmark。
3. 没有稳定 benchmark，不进入微调和架构扩展。
4. 没有最简单基线，不声明新模型或新架构有效。
5. 没有失败分类，不优化 prompt、路由或数据。
6. 没有独立验证，不把 AI 辅助产物计入能力完成度。

### 1.9.1 评测资产分级

| 层级 | 用途 | 最低要求 | 允许得出的结论 |
|---|---|---|---|
| 诊断样例 | 暴露个人认知缺口 | 3 至 5 个问题或实验，可记录不会 | 只能决定补什么，不评价模型优劣 |
| 评测练习集 | 学习任务定义和评分 | 输入、预期行为、证据、拒答与失败标签明确 | 只能判断 rubric 是否可用 |
| benchmark 草案 | 建立模型/方案基线 | 可复现命令、最简单基线、重复评分检查 | 可形成初步错误分类，不宜做稳定能力声明 |
| 冻结 benchmark | 回归、方案比较和阶段验收 | 版本冻结、评分一致性、分组覆盖、变更记录 | 可用于受边界约束的比较结论 |

case 数量只是结果，不是升级条件。若任务定义、证据边界或评分一致性不成立，不得通过继续增加 case 掩盖问题。

### 1.9.2 防止 vibe coding

每个关键能力至少满足以下三项中的两项：

1. 不使用 AI 完成最小实现或核心模块重写。
2. 给定故障能够独立定位并修复。
3. 能解释设计取舍并通过反例说明边界。

所有阶段产物至少包含以下两类证据：

- 代码与测试。
- 数据与指标。
- 运行日志与 profile。
- 失败样本和根因分析。
- 独立 review 或闭卷复述记录。

以下不计入完成度：

- 只完成课程或阅读。
- 只运行开源 Demo。
- 只由 AI 生成代码而没有独立验证。
- 只有测试数量，没有真实任务指标。
- 只有架构文档，没有运行证据。

### 1.9.3 完成度评分

每个阶段按 0 至 2 分记录五个维度：

| 维度 | 0 分 | 1 分 | 2 分 |
|---|---|---|---|
| 理解 | 只能复述 | 能解释主流程 | 能解释取舍与反例 |
| 实现 | 未运行 | 可运行 | 有测试且可维护 |
| 评测 | 无指标 | 有总体指标 | 有分组、失败与基线 |
| 调试 | 依赖外部修复 | 能按提示修复 | 能独立定位根因 |
| 表达 | 无文档 | 有过程记录 | 可完成系统讲解与答辩 |

Phase 1 及之后的阶段总分至少 8 分，且“评测”和“调试”均不得为 0，才允许进入下一阶段。Phase 0 使用其独立退出条件，不要求为了获得评测分而提前制造 benchmark。

## 1.10 双周执行与月度复盘

### 1.10.1 双周循环

每两周执行一次：

1. 冻结一个交付物和一个成功指标。
2. 学习早期先建立教学骨架和可观察结果，再用诊断题检查吸收情况；进入实验阶段后再建立基线与复现命令。
3. 实现最小变更并保存失败样本。
4. 做一次独立解释、重写或故障定位验证。
5. 根据证据决定继续、缩小或停止，不按投入时长判断完成。

### 1.10.2 月度复盘

每月回答：

1. 本月新增了什么可验证能力？
2. 哪个能力仍只能依赖 AI 或文档完成？
3. 哪个实验推翻了原先判断？
4. 当前旗舰项目的最大失败模式是什么？
5. 下月只保留哪一个最高优先级交付？

记录字段：

| 字段 | 内容 |
|---|---|
| 本月目标 | |
| 完成产物 | |
| 验证命令或指标 | |
| 独立完成部分 | |
| AI 辅助部分 | |
| 失败与缺口 | |
| 下月唯一主目标 | |

## 1.11 前四周启动清单

### 1.11.1 第 1 周

- 完成覆盖 LLM、Transformer、VLM、Agent 和评测的能力矩阵。
- 闭卷画出目前理解的 LLM 最小推理数据流；不会的节点直接标记为缺口。
- 从已有 DMS/OMS 项目中选定一个合规、可复现的多模态问题。
- 写一页 problem statement 和非目标。

### 1.11.2 第 2 周

- 使用最小代码或可观察模型打通 `文本 → token → embedding → causal attention → logits → decoding → 文本`。
- 完成一次采样参数对照实验和一次有证据/无证据对照实验。
- 输出阻塞项分级，并据此冻结 Phase 1 的第一个双周交付物。

### 1.11.3 第 3 至 4 周

- 补齐 Phase 0 暴露出的 LLM 核心机制缺口。
- 独立实现并测试 scaled dot-product attention；是否扩展为完整最小 Transformer 由诊断结果决定。
- 完成解码参数、上下文边界和证据约束的受控实验，为第 6 周开始的评测练习建立概念基础。
- 不采集正式 benchmark，不以运行开源 VLM Demo 作为前四周完成条件。

## 1.12 当前未解决项

- 需要完成 Phase 0 后，根据 LLM/VLM 能力缺口确定 Phase 1 各子阶段实际时长。
- 需要完成评测练习和 rubric 一致性检查后再确定首个 VLM 与具体数据集，避免先选框架或先堆 case 后定义问题。
- 需要确认座舱图像、视频、车辆状态和问题分析数据来源合法且可公开说明。
- 需要根据实际可用 GPU、板端设备和云端预算调整模型规模。
- 当前阶段时长按每周 10 至 12 小时估计，尚未用真实执行节奏校准。
- 目标岗位应在完成 Phase 2 后结合实际招聘要求再次校准。

## 1.13 隐私维护规则

- 本方案可以保留已授权的 DMS、EyeStatus、Agent workflow、agent-trajectory 等项目名称、技术事实和验证证据。
- 不记录学历、雇主、任职时长、个人与项目的任职关系、未公开数据或可识别个人身份的组合信息。
- 引用已有项目时必须区分“项目中已经存在的能力”与“已经独立掌握的能力”，后者只能由本方案规定的验证证据确认。
- 求职材料中的个人信息与本知识库学习方案分离维护。
