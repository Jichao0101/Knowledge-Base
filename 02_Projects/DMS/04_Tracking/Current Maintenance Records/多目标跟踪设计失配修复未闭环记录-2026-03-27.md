---
title: DMS 多目标跟踪设计失配修复未闭环记录 2026-03-27
summary: 记录本轮 face/hand 生命周期解耦与 handoff 清理修复的实现状态、独立审查结论和未闭环原因。当前代码已完成设计向修正并通过编译，但由于缺少核心生命周期场景验证，任务按流程停在 verification_blocked，不进入知识闭环收尾。
sources:
  - 01_Knowledge/Agent Workflow/Agent驱动知识库与代码库协同闭环规范.md
  - 01_Knowledge/Agent Workflow/Agent双侧运行规范与调度模板.md
  - 02_Projects/DMS/04_Tracking/座舱乘员多目标跟踪方案.md
  - /home/jichao/dms/include/utils/track.h
  - /home/jichao/dms/source/utils/track.cpp
scope: 适用于本轮 DMS tracking design_mismatch 修复任务的中间状态记录，不表示任务已闭环完成。
risks:
  - 核心 handoff 路径未做场景级验证，不能证明 retired body anchor 清理逻辑在真实序列上按预期触发。
  - allocateBodyTrackId 继续避让 face/hand 活跃 id，存在低级别分配副作用风险，尚未通过运行验证评估。
status: pending_review
doc_role: delta
truth_role: evidence
lifecycle_state: superseded
default_entry: false
sync_required_when: []
retrieval_priority: evidence_only
supersedes: []
merged_into:
  - 02_Projects/DMS/04_Tracking/tracking_validation_current.md
current_replacement:
  - 02_Projects/DMS/04_Tracking/tracking_validation_current.md
related_code:
  - /home/jichao/dms/include/utils/track.h
  - /home/jichao/dms/source/utils/track.cpp
record_type: investigation_record
target_current_docs:
  - 02_Projects/DMS/04_Tracking/tracking_validation_current.md
decision_scope: DMS Tracking blocked verification state
updated_at: 2026-04-03
---

> 文档状态：本文件记录一次未闭环状态，后续状态已被 `多目标跟踪手部连续性优化闭环记录-2026-03-31` 和 `tracking_validation_current` 覆盖；保留审计价值，不再参与默认当前态检索。

## 0.1 Retrieval Summary

- topic: face/hand 生命周期解耦与 handoff 清理修复的未闭环状态。
- anchors: `verification_blocked`, `m_retiredBodyTracks`, `handoff`, `orphan child`, `allocateBodyTrackId`。
- state: 代码已完成设计向修正并通过编译，但 reviewer 未给出 `pass`。
- blocker: 缺少 body disappear、new stable body handoff 和 left/right hand 独立失配等核心场景验证。
- boundary: 本记录不能作为修复完成结论，只能用于追溯 verification blocked 原因。

## 0.2 Task

- task_type: `bug_fix.design_mismatch`
- target: 生命周期管理、存在解耦机制、唯一原则符合《座舱乘员多目标跟踪方案》

## 0.3 Expected vs Actual

### 0.3.1 Expected

- `body` 丢失时不应反向清理已启动的 `face/hand`
- `face/hand` 启动后可基于自身预测继续更新
- `hand` 删除粒度为 `left/right` 子轨迹
- 新 stable `body` 在同一区域出现时，旧 `bodyId` 遗留的 `face/hand` 应清理并由新 `bodyId` 重新初始化

### 0.3.2 Actual Before Rework

- `body` miss 时直接清理关联 `face/hand`
- `body` 不在当前输出时直接删除整组 hand 状态
- `hand` 生命周期删除对象是整组 `BodyHandState`
- 旧 child 清理最初依赖几何命中，后续返工前仍存在 handoff 判定不充分问题

## 0.4 Code State

- 已完成 `face/hand` 与 `body` 的存在解耦
- 已完成 `hand left/right` 独立 miss 衰减与删除
- 已加入 `m_retiredBodyTracks`，在 `body` 删除时保留旧 body 历史锚点
- 新 stable `body` 出现时，按 retired body anchor 清理 orphan `face/hand`，不再依赖 child 当前几何或 `stablePersonType`

## 0.5 Verification

- 已执行：
  - `bash scripts/compile_j6b.sh`
- 结果：
  - 编译通过
  - `Built target gTestsdk`
- 未执行：
  - `body disappear -> child decoupled self-maintain`
  - `new stable body same-region handoff -> old child clear -> new child reinit`
  - `left/right` hand 独立失配与重建场景验证

## 0.6 Independent Review

- reviewer_round_1: `rework_needed`
  - 指出“无 stable body 时的唯一性解读”和验证不足
- reviewer_round_2: `rework_needed`
  - 指出 handoff 清理仍依赖当前几何，不足以覆盖 orphan child 漂移场景
- reviewer_round_3: `rework_needed / partially_closed`
  - 指出即使代码契约基本对齐，仍缺少关键生命周期路径的运行验证

## 0.7 Closure Decision

- 当前状态：`verification_blocked`
- 不进入 `knowledge-closer`
- 不写“修复完成”结论

原因：

- 根据任务停止条件，当前属于“无法验证核心改动”
- 独立 reviewer 未给出 `pass`

## 0.8 Next Action

- 需要补最小场景级验证入口后，再重新执行独立审查
- 在 review `pass` 前，不应将本条目视为修复闭环记录
