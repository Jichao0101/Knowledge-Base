---
type: project_maintenance_record
status: completed_with_gaps
project: DMS
module: Issue Analysis Skill
scope: 审查用户提供的 R 核远程规则更新，按确认修正后同步本地项目来源，并记录 Skill reference/strategy 尚未对齐的缺口。
sources:
  - 2026-07-29 用户提供的 R 核远程文档更新内容
  - 2026-07-29 用户确认的优先级、信号名、灵敏度和头部姿态处理口径
  - 02_Projects/DMS/10_Issue_Analysis_Skill/DMS R核问题分析流程.md
created_at: 2026-07-29
updated_at: 2026-07-29
---

# R 核远程规则更新审查与本地同步

## 1. 本轮结论

- 远程更新初稿存在遮挡/无人脸优先级表述矛盾、抽烟误报措辞笔误、危险报警信号名变化和“非低灵敏度”边界过宽等问题，因此未直接覆盖本地文件。
- 用户确认修正口径后，已将更新后的规则同步到本地项目来源文档。
- 本轮只更新知识库授权模块；未修改 `/mnt/d/analyze-dms-issue` 中的 Skill reference/strategy，也未宣称 R 核 analyser 或运行能力发生变化。

## 2. 已同步规则

- 闭眼：Normal/High 为 2s、Low 为 5s；轻度疲劳允许多次不超过 150ms 的间歇 `false`，重度疲劳允许多次不超过 200ms 的间歇 `false`。
- 打哈欠：High 为 3s，允许多次不超过 150ms 的间歇 `false`；Low 无该功能，Normal 规则仍未说明。
- 视线分心：Normal/High 为 3s，允许多次不超过 100ms 的间歇 `false`。
- 头部姿态异常：Normal/High 为 3s、Low 为 60s，允许多次不超过 300ms 的间歇 `false`；保留“当前状态标定关闭”。
- 接打电话：High 为 3s，允许多次不超过 200ms 的间歇 `false`；其余灵敏度无该功能。
- 抽烟：High 为 2s，允许多次不超过 200ms 的间歇 `false`；其余灵敏度无该功能，并补齐独立冷却期、速度和报警优先级排查。
- 危险报警信号统一采用 `ADASDMSDangrsDrvgBhvrWrngIndReq`。
- 遮挡与无人脸分别记录为高于疲劳、分心和抽烟/接打电话报警；未推断两者之间的相对优先级。
- “非低灵敏度”统一收敛为 Normal/High，避免把 Off、Unknown 和 Reserved 纳入功能有效范围。

## 3. 来源快照变化

- 同步前 SHA-256：`228a09cea3cdfa44cef06083dc2e18e2b9773d7d1500eb9df9e57b9c56b567f3`
- 同步后 SHA-256：`2d436f92572391fb373117dc3de3c2a77d199721ccfb67dbce6348dfa16ddee9`
- 既有 `r-core-analysis-strategy.json` 锁定同步前快照。完成 reference/strategy 差异转换、策略版本更新、来源 hash 更新和回归验证前，当前状态记为 `source_updated/reference_sync_pending/analyser_not_implemented`。

## 4. 保留边界

- 普通灵敏度哈欠规则仍为 `not_documented`。
- `isMonitoringEnabled` 只在信号定义中出现，其与分心判定的门禁关系仍为 `decision_relation_not_documented`。
- 头部姿态异常规则按新来源保留，但当前标定关闭状态不变。
- R 核 analyser、MF4/信号输入适配、证据 ID、结果 schema 和原子输出仍未实现，运行阶段继续为 `skipped/r_core_analyser_not_available`。
- 未完成独立 recoverability verification，未声明 `single_pass_recoverable: true`。

## 5. 验证

- 写入前 trace-index 已限定在本模块授权路径。
- 四个目标的语义 preflight 均为 `manual_review`；用户对争议项的逐项确认作为本次显式 review。
- `git diff --check`：通过。
- 来源文档同步后 SHA-256 已重新计算并记录。
- 授权路径 lint 的全库入口缺失提示属于路径受限检查结果；本轮要求以目标文件新增回归为判断口径。

## Retrieval Summary

R 核项目来源已按用户确认的新规则同步，补充六类检测的间歇容忍窗口和完整抽烟规则。Skill reference/strategy 尚未跟随新来源更新，R 核运行状态仍为 skipped。

## Retrieval Anchors

- `source_updated`
- `reference_sync_pending`
- `ADASDMSDangrsDrvgBhvrWrngIndReq`
- `intermittent_false`
- `isMonitoringEnabled`
- `r_core_analyser_not_available`
