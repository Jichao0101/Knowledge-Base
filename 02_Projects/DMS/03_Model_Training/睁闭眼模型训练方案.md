# 1 睁闭眼分类数据与实验方案优化版

## 1.1 目标与约束

本文档用于统一当前睁闭眼分类任务的数据构建、输入生成、训练配置、评测协议与误分类分析机制，确保后续实验具备以下性质：

- 训练输入与部署输入尽可能一致
- 数据版本、split 版本、输入版本、训练版本、评测版本严格绑定
- 增量数据加入后，历史 benchmark 不漂移
- 输入实验、loss 实验与 backbone 实验可归因
- 误分类可回溯至原始数据与输入生成链路
- 正式 baseline 以量化表现优先，而非仅以 FP32 最优为准

本文档同时明确当前阶段的边界条件：

- split 主键仅使用 `group_id = f(person_id)`
- 其他信息仅保留为 `meta`，不参与 split
- 仅使用 `total_samples >= N_min` 作为评测 group 候选门槛
- 暂不引入时序约束、覆盖度约束、受约束分配
- 暂不处理连续抽帧导致的有效样本数虚高问题
- 评测仅使用 **GT 口径**
- 不再保留 `Deploy Eval` 作为正式输出口径

---

## 1.2 当前真正需要解决的问题

当前正式方案聚焦解决以下问题：

1. 训练输入与部署输入不一致  
2. 训练不能再以 `reflect / replicate padding` 为正式主线  
3. ROI 过扁导致 padding 占比过高，模型可能学习 padding shortcut  
4. 输入实验变量混杂，需要拆开“减 padding”和“加上下文”两个效应  
5. split 不支持增量公平比较，必须冻结 `val/test`  
6. 稀疏 group 不适合作为正式评测单元，需要最小样本门槛  
7. MobileNetV2 基线尚未正式重建，必须先稳定 backbone  
8. baseline 选择必须纳入量化表现，而不能只看 FP32  
9. `eye_narrow` 与 `eye_occluded` 的瓶颈机制不同，不能混合优化  
10. 误分类分析必须进入正式闭环，而不能只是附属功能  
11. 训练、量化、部署前处理协议必须写死，避免“表面对齐、细节失配”  
12. 每个正式实验必须绑定完整版本信息  

---

## 1.3 原始数据集

### 1.3.1 理论数据量

睁闭眼数据集来自 56 个场景，每个场景为光线、位置、遮挡的组合，包含：

- 常规场景 12 个，每个场景 24 个 id，每个 id 执行 14 个动作，单 id 抽帧 `14 * 20 = 280` 帧
- 复杂场景 44 个，每个场景 2 个 id，每个 id 执行 14 个动作，单 id 抽帧 `14 * 20 = 280` 帧

总数据量：

``````text
(12 * 24 + 44 * 2) * 280 = 105280
``````

说明：

- 该值为理论样本量，不代表最终可用样本量
- 最终可用样本量受以下因素影响：
  - 图片是否存在
  - JSON 是否可解析
  - bbox 是否有效
  - bbox 是否越界
  - 标签是否合法
  - split 过滤规则
  - ROI 导出是否成功

### 1.3.2 数据标签格式

``````json
{
  "filePath": "xxxxxxxxxxx.jpg",
  "label_path": "",
  "info": {
    "depth": 3,
    "height": 1080,
    "width": 1920
  },
  "dataList": [
    {
      "coordinates": [
        [497, 495],
        [641, 639]
      ],
      "id": 2,
      "properties": {
        "eye_status": "eye_closed"
      }
    },
    {
      "coordinates": [
        [672, 441],
        [817, 558]
      ],
      "id": 1,
      "properties": {
        "eye_status": "eye_closed"
      }
    }
  ]
}
``````

### 1.3.3 数据命名规范

新数据命名示例：

``````text
APillar_C1UL_seat1_2025122007_f_155_48_Black_LargeYawPose_NoOcclusion_c1IR1_StaticOutdoor_SunnyNormal_2137_NormalScene_000006.jpg
``````

字段含义：

``````text
摄像头位置_车型_驾驶位置_模特编号_性别_身高_年龄_动作_配饰_摄像头_光线场景_天气_日照值_角度_序号
``````

历史数据命名示例：

``````text
seat1_2025090705_m_181_23_NormalDriving_Glasses_c1R1_StaticOutdoor_CloudyDay_Normalscene_000005.jpg
``````

字段含义：

``````text
摄像头位置_模特编号_性别_身高_年龄_动作_配饰_车型_光线场景_天气_角度_序号
``````

说明：

- 新老数据命名字段不完全一致
- 后续 `meta` 解析必须允许字段缺失
- 文件名仅用于提取候选信息，不作为唯一可信来源
- `person_id` 提取规则需要版本化

---

## 1.4 数据集构建总原则

### 1.4.1 基本原则

- 样本粒度：**单眼**
- split 粒度：**group**
- `group_id = f(person_id)`
- split 比例：**8:1:1**
- seed 固定
- 评估必须可复现：同一版本数据集 + 同一 seed + 同一 split 策略 + 同一输入生成策略 ⇒ 导出结果一致

### 1.4.2 当前 split 约束

正式实验当前只采用以下 split 规则：

- 仅从文件名、路径、JSON 中抽取 `person_id`
- 统一映射为 `group_id = f(person_id)`
- 按 `group_id` 分配 `train / val / test`
- 同一 `group_id` 下所有图片与单眼样本广播到同一 split
- 其他信息如 `session_id`、`action_id`、`scene`、`camera`、`illumination` 仅保留在 `meta` 中，不参与 split

### 1.4.3 当前不纳入的机制

以下机制在当前阶段明确不启用：

- 不使用时序约束
- 不使用覆盖度约束
- 不使用受约束分配
- 不单独修正连续抽帧导致的有效样本数虚高

理由：

- 当前首要目标是先建立一套可复现、可归因、便于执行的正式闭环
- 若同时引入过多 split 约束，会显著增加构建复杂度与执行成本
- 当前阶段优先解决输入一致性、baseline 重建、量化主导与误分类闭环问题

---

## 1.5 采样与 split 策略

### 1.5.1 group split

正式实验默认使用 **group split**：

- 从文件名、路径、JSON 中提取 `person_id`
- 生成 `group_id = f(person_id)`
- 按 `group_id` 分配 split（8:1:1）
- 广播到该 group 下全部图片与单眼样本
- 输出：
  - `split_report.json`
  - `leakage_audit.txt`

要求：

- `group_id` 跨 split 次数必须为 0

### 1.5.2 评测 group 候选门槛

为避免极小 group 进入 `val/test` 造成评测波动，当前采用最简门槛规则：

- 若某 `group_id` 的 `total_samples < N_min`，则该 group 默认只进入 `train`
- 只有 `total_samples >= N_min` 的 group 才允许作为 `val/test` 候选

说明：

- 当前仅使用 `total_samples` 作为门槛
- 暂不引入时序去相关后的有效样本数约束
- 暂不引入场景覆盖度约束

建议输出：

``````text
group_registry.json
``````

字段至少包括：

- `group_id`
- `person_id_raw`
- `total_samples`
- `is_eval_candidate`
- `split`
- `first_seen_source_version`
- `is_frozen`

### 1.5.3 冻结评测集与增量更新原则

为保证版本间实验可比，正式实验采用 **冻结评测集** 策略：

- 历史 `val/test` 中的 `group_id` 一旦确定，不允许在后续增量版本中流入 `train`
- 新数据到来时，历史 `group_id` 继承原 split，不重新随机切分
- 新增且 `total_samples < N_min` 的 `group_id` 默认只进入 `train`
- 仅在显式创建新的 benchmark 版本时，才允许重建 `val/test`

说明：

- `split_version` 必须可追溯
- benchmark 变化必须通过新版本体现，而不能隐式覆盖旧版本

---

## 1.6 数据集构建流程

### 1.6.1 Stage A：扫描源目录，生成 source manifest

#### 1.6.1.1 目标

低 I/O 找全 JSON，并行完成 JSON 与 bbox 语义校验，同时为后续 group split 提供 `person_id` 候选信息。

#### 1.6.1.2 输入

- `annotation_root`（只读）

#### 1.6.1.3 扫描规则

- 单线程 `os.walk`
- 只在命中 `BlinkDetection` 子树时收集 `*.json`

#### 1.6.1.4 并行检查

- JSON 能否解析
- 是否有 `filePath`
- 是否有 `info.width/height`
- `dataList` 是否为 list
- 每个 `dataList[i]`：
  - `properties.eye_status` 是否合法
  - `coordinates` 是否为 `[[x1,y1],[x2,y2]]`
  - 坐标是否可转 float
  - 坐标是否非 `NaN/Inf`
  - `x2 > x1, y2 > y1`
  - 宽高是否大于 `min_size`
  - 若有 `width/height`，检查越界情况
- 提取 `person_id_guess`
- 记录命中规则版本 `person_parse_rule_version`

#### 1.6.1.5 产出

1. `blink_roots.txt`
2. `source_manifest.json`
3. `data_checklist.txt`
4. `group_key_stats.json`

其中 `source_manifest.json` 每条记录至少包含：

- `json_rel`
- `img_rel_guess`
- `filePath`
- `samples`
- `validation_stats`
- `person_id_guess`
- `person_parse_rule_version`

说明：

- Stage A 不读图片
- 只做低成本结构检查
- 缺图只能在后续阶段确认

### 1.6.2 Stage B：复制数据到私人目录

#### 1.6.2.1 输入

- `source_manifest.json`

#### 1.6.2.2 动作

- 复制 JSON 到 `DEST/<json_rel>`
- 推导图片路径：`img_abs = json_abs.parent / filePath`
- 若图片存在，则复制到 `DEST/<img_rel_guess>`
- 若不存在，则记录到 `missing_images.txt`

#### 1.6.2.3 产出

- `missing_images.txt`
- `copy_report.json`
- `copied_jsons.txt`
- `copied_images.txt`

### 1.6.3 Stage C：容器内升级 final manifest

#### 1.6.3.1 样本粒度与 split 粒度

- 样本粒度：单眼
- split 粒度：group
- `group_id = f(person_id)`
- split 比例：8:1:1
- seed 固定

#### 1.6.3.2 数据检查

- 再次解析 JSON，防复制过程损坏
- 读图获取真实 `W/H`
- bbox 越界按真实 `W/H` 判定
- `img_path/json_path` 统一写为 `dest` 相对路径
- `image_id = sha1(img_rel)`，绑定同图双眼
- `group_id = f(person_id)`
- 基于 `group_id` 生成 split，并广播到组内全部样本

#### 1.6.3.3 输出

- `final_checklist.txt`
- `split_report.json`
- `leakage_audit.txt`
- `group_registry.json`
- `final_manifest.json`

其中 `final_checklist.txt` 至少统计：

- 真实缺图率
- 读图失败率
- bbox 越界率
- label 分布
- split 分布
- `val/test` group 最小样本数
- 因 `total_samples < N_min` 被降入 `train` 的 group 数量

### 1.6.4 manifest 字段设计

#### 1.6.4.1 build 总字段

建议包含：

- `source_version`
- `split_version`
- `input_version`
- `manifest_version`
- `train_cfg_version`
- `eval_protocol_version`
- `preprocess_spec_version`
- `quant_calib_version`
- `person_parse_rule_version`
- `seed`
- `split_ratio`
- `split_policy`
- `filter_rule`
- `timestamp`
- `label_dist_per_split`
- `group_dist_per_split`
- `sample_dist_per_split`
- `N_min`

#### 1.6.4.2 单样本字段

每条样本建议包含：

- `img_path`
- `json_path`
- `image_id`
- `group_id`
- `person_id_raw`
- `eye_id`
- `bbox_gt`
- `label`
- `split`
- `meta`

#### 1.6.4.3 meta 字段

当前至少保留：

- `scene`
- `camera`
- `pose`
- `illumination`
- `weather`
- `accessory`
- `seat`
- `action`
- `session_id`（若可解析）
- `frame_idx`（若可解析）

说明：

- 上述字段当前仅用于分析与审计
- 不作为 split 约束条件

---

## 1.7 预处理协议固化

这是正式方案新增的关键部分。为避免训练、量化、部署在实现细节上失配，预处理协议必须版本化并写死。

建议新增文件：

``````text
preprocess_spec.yaml
``````

### 1.7.1 必须固化的字段

- `color_order`：`RGB` / `BGR`
- `decode_backend`
- `crop_coord_rounding`：`floor / round / ceil`
- `resize_backend`
- `resize_interpolation`
- `keep_ratio`
- `pad_mode = const`
- `pad_value_raw`
- `pad_apply_stage = before_normalize`
- `normalize_mean`
- `normalize_std`
- `input_size`
- `dtype_cast_order`
- `quant_input_dtype`
- `quant_calib_sampling_rule`

### 1.7.2 当前正式要求

训练、量化、部署前处理协议必须满足：

- padding 模式统一为 `const`
- pad value 写入正式版本，不允许口头约定
- `RectROI` 与 `SquareCrop` 的坐标取整规则一致
- 量化 calibration 的输入协议与训练/评测保持一致
- 所有正式实验必须记录 `preprocess_spec_version`
- 所有正式量化实验必须记录 `quant_calib_version`

### 1.7.3 作用机制说明

这一步的目的不是增加配置数量，而是消除以下隐性漂移：

- 相同 bbox 在不同链路中的取整差异
- 相同图片在不同 resize 实现中的插值差异
- 相同 const padding 在不同阶段施加导致分布变化
- 训练期 normalize 与量化期 calibration 分布不一致

若这部分不固化，则即使表面上都叫“ConstPad + 128×128”，仍然可能不是同一输入协议。

---

## 1.8 输入生成策略

### 1.8.1 正式输入主线

由于部署侧仅支持 **const padding**，正式实验输入主线统一为以下两类：

#### 1.8.1.1 A. RectROI + Const Padding

基于 `bbox_gt` 裁剪眼部矩形 ROI，再进行 keep-ratio resize 与 constant padding 到方形。

用途：

- 作为部署对齐基线
- 作为最小可解释输入方案

#### 1.8.1.2 B. SquareCrop + OOB Const Padding

基于标注框中心 `(cx, cy)`，从原图直接裁取正方形区域：

``````text
crop_size = k * max(w_bbox, h_bbox)
``````

若裁剪越界，仅对越界区域做 constant padding。

用途：

- 减少人工 padding 占比
- 降低 aspect ratio shortcut
- 引入真实眼周上下文
- 更贴近“中心点 + 固定方窗”的部署前处理

### 1.8.2 输入实验顺序

为避免“减少 padding”和“增加上下文”两个效应混在一起，输入实验按两步推进：

#### 1.8.2.1 第一步：验证减 padding 效应

- `RectROI + ConstPad`
- `SquareCrop(k=1.0) + OOB ConstPad`

#### 1.8.2.2 第二步：验证加上下文效应

- `SquareCrop(k=1.4)`
- `SquareCrop(k=1.8)`
- `SquareCrop(k=2.2)`

解释：

- `k=1.0` 用于隔离“方形化 + 减 padding”效应
- `k>1.0` 用于评估额外上下文是否有助于 `eye_occluded`

### 1.8.3 ROI shortcut 观测字段

由于已经识别到模型可能利用 padding 占比、边框面积、边框位置等伪特征，因此 ROI 导出阶段必须显式记录这些观测变量。

建议在 `roi_manifest.json` 中新增：

- `crop_mode`
- `crop_w`
- `crop_h`
- `square_size`
- `bbox_w`
- `bbox_h`
- `bbox_area`
- `crop_area`
- `bbox_aspect_ratio`
- `bbox_area_ratio`
- `content_area_ratio`
- `pad_area_ratio`
- `pad_left`
- `pad_right`
- `pad_top`
- `pad_bottom`
- `oob_pad_ratio`
- `context_ratio`

#### 1.8.3.1 这些字段的作用

`pad_area_ratio` 用于衡量该样本中有多少像素来自 padding。若误分类随着该值增加而显著上升，则说明模型确实受 padding shortcut 影响。

`bbox_aspect_ratio` 用于衡量原始眼框是否过扁。若极端长宽比样本更易误分，则说明 RectROI 路线可能放大了形状偏差。

`context_ratio` 用于衡量裁剪区域相对 bbox 的上下文比例。它是分析 `occluded` 是否受益于更大真实上下文的关键变量。

### 1.8.4 Bounding Box 离线增强策略

#### 1.8.4.1 核心原则

- bbox 增强发生在裁剪 ROI 之前
- 用于模拟部署时“关键点 / 检测框 → 外接框”的偏差
- 训练集随机扰动
- 验证 / 测试集固定规则，保证可复现
- 当前正式评测仍只使用 **GT 口径**

#### 1.8.4.2 训练集 jitter（随机）

对 `B_gt` 随机扰动得到 `B_aug`，再裁剪：

- 外扩/内缩：`pad_ratio ∈ [-0.10, 0.15]`
- 平移：`dx, dy ∈ [-0.03, 0.03] × bbox_size`

约束：

- 最小宽高 `min_size`
- clamp 到图像边界
- 不重采样，失败则丢弃并统计

命名建议：

``````text
{image_id}__{eye_id}__aug{0..K-1}__{label}.jpg
``````

随机数使用样本级确定性随机：

``````text
rng_seed = sha1(seed, image_id, eye_id, aug_id)
``````

#### 1.8.4.3 验证 / 测试

当前正式评测只保留 **GT 口径**：

- `pad_ratio = 0.0`
- 或直接使用 `bbox_gt`
- 用于观察理想输入下的模型能力
- 不单独输出 `Deploy Eval`

### 1.8.5 ROI 导出索引

生成：

``````text
roi_manifest.json
``````

#### 1.8.5.1 build 字段建议

- `source_version`
- `split_version`
- `input_version`
- `manifest_version`
- `preprocess_spec_version`
- `seed`
- `split_ratio`
- `filter_rule`
- `bbox_aug_cfg`

#### 1.8.5.2 每个 ROI 条目建议包含

- `roi_path`
- `img_path`
- `json_path`
- `image_id`
- `group_id`
- `eye_id`
- `rng_key`
- `bbox_used`
- `bbox_gt`
- `split`
- `label`
- `crop_mode`
- `pad_ratio`
- `dx_ratio`
- `dy_ratio`
- `bbox_aspect_ratio`
- `bbox_area_ratio`
- `content_area_ratio`
- `pad_area_ratio`
- `oob_pad_ratio`
- `context_ratio`
- `meta`

---

## 1.9 输出目录结构

``````text
EyeStatusData_v{manifest_version}/
  train/
    eye_open/
    eye_closed/
    eye_occluded/
    eye_narrow/
  val/
    eye_open/
    eye_closed/
    eye_occluded/
    eye_narrow/
  test/
    eye_open/
    eye_closed/
    eye_occluded/
    eye_narrow/
``````

版本名中应编码输入策略，建议采用部署对齐命名：

``````text
EyeStatusData_RectROIConst114_v1
EyeStatusData_RectROIConst0_v1
EyeStatusData_SquareCropK10Const114_v1
EyeStatusData_SquareCropK14Const114_v1
EyeStatusData_SquareCropK18Const114_v1
EyeStatusData_SquareCropK22Const114_v1
``````

说明：

- `Const114` 与 `Const0` 不能只体现在文件夹命名中
- 对应的 `pad_value_raw` 必须写入 `preprocess_spec.yaml`

---

## 1.10 模型结构

- 采用 **MobileNetV2** 作为基础网络
- 使用 ImageNet 预训练权重初始化
- 修改最后分类层为 **4 分类**
  - `eye_closed = 0`
  - `eye_narrow = 1`
  - `eye_occluded = 2`
  - `eye_open = 3`

说明：

- 原 `MobileNetV3` 因量化掉点严重，不再作为正式实验主干
- 当前所有正式对照实验均以 **MobileNetV2** 为统一 backbone
- 历史基于 V3 的实验结论仅保留为参考，不直接用于当前正式结论

---

## 1.11 模型输入与数据增强

### 1.11.1 总体原则

训练侧保持“输入生成策略”和“在线增强策略”分层：

1. Stage D 生成基础输入（`RectROI` 或 `SquareCrop`）
2. 训练代码中执行轻量在线增强

目的：

- 固定输入构造版本，便于对照实验归因
- 将输入生成策略与在线增广解耦
- 保证部署前处理与训练输入更容易对齐

### 1.11.2 Stage T1：V2 基线重建时的训练增强

在 **V2 基线重建阶段**，训练配置应尽量简化，先建立最小可解释基线。

#### 1.11.2.1 推荐保留

1. `Resize` 或 `ResizeKeepRatio`
2. `PadToSquare`（仅 `RectROI` 方案需要）
3. `RandomHorizontalFlip`
4. 轻量 `ColorJitter`
5. `ToTensor`
6. `Normalize`

#### 1.11.2.2 暂不启用

- `RandomErasing`
- 强 bbox jitter
- Weighted CE
- Focal Loss
- Label Smoothing
- 多输入策略混跑

原因：

- 先把 backbone 的训练动力学与量化表现调稳
- 避免增强、loss、输入策略同时变化，导致结论不可归因

### 1.11.3 Stage T2/T3/T4：对照实验阶段

在 T1 完成后，若基线已稳定，可在正式对照实验中继续沿用轻量在线增强。

推荐默认增强链路：

1. `ResizeKeepRatio(long=128)` 或 `Resize(128, 128)`
2. `PadToSquare(128)`（仅 `RectROI` 方案需要）
3. `RandomHorizontalFlip`
4. 轻量 `ColorJitter`
5. `ToTensor`
6. `Normalize`

说明：

- `RandomErasing` 默认仍关闭
- 仅当后续单独验证其收益时再启用

### 1.11.4 验证 / 测试

要求：

1. 与训练保持相同输入构造逻辑
2. 不使用随机增强
3. 同版本、同 seed、同配置下结果可复现
4. 统一使用 **GT 口径**

当前正式评测定义如下：

- 使用 `bbox_gt`
- 或使用 `pad_ratio = 0.0` 的等价 GT 裁剪逻辑
- 不单独输出 `Deploy Eval`
- 所有正式指标均基于 GT 口径统计

---

## 1.12 训练配置

### 1.12.1 训练基线重建目标

由于 backbone 已从 `MobileNetV3` 切换到 `MobileNetV2`，原先基于 V3 的训练超参数不再直接适用。

因此正式训练流程更新为：

1. 先完成 **T1：MobileNetV2 基线重建**
2. 再开展 loss 对照实验
3. 再开展输入构造对照实验
4. 最后进行联合优化验证

### 1.12.2 Stage T1：MobileNetV2 基线重建配置

#### 1.12.2.1 固定项

- Backbone：`MobileNetV2`
- Loss：`CrossEntropyLoss`
- 单一部署对齐输入基线
- 输入尺寸：`128 × 128`
- Batch size：`64`
- Optimizer：`AdamW`
- Scheduler：`CosineAnnealingLR`
- Epoch：`100`
- AMP：开启
- DDP：关闭

#### 1.12.2.2 搜索项

Learning Rate：

- `3e-4`
- `6e-4`
- `1e-3`

Weight Decay：

- `1e-5`
- `1e-4`
- `5e-4`

EMA：

- `on`
- `off`

#### 1.12.2.3 选择标准

统一记录：

- `FP32 Macro-F1`
- `Quant Macro-F1`
- `Drop = FP32 - Quant`

优先级：

1. `Quant Macro-F1` 更高
2. `Drop` 更小
3. 训练过程更稳定
4. FP32 与量化结果一致性更好

### 1.12.3 Stage Gate 机制

目的是避免在不稳定基线上继续扩展 loss 或输入实验。

#### 1.12.3.1 T1 -> T2/T3 进入条件

只有满足以下条件，才进入 loss 或输入构造对照阶段：

- 已确定单一 `MobileNetV2` baseline
- `Quant Macro-F1` 已稳定
- `Drop` 可接受
- 同配置重复实验结果无明显漂移
- GT 口径下 confusion matrix 主迁移方向稳定

#### 1.12.3.2 T2（loss）结束条件

只有在以下条件成立时，某 loss 方案才视为优于 baseline：

- `Quant Macro-F1` 持续高于 CE baseline
- `eye_open` 的 Precision 未明显恶化
- confusion matrix 主迁移方向未显著恶化

#### 1.12.3.3 T3（input）结束条件

输入方案优化必须回答两个问题：

1. `SquareCrop(k=1.0)` 是否验证“减 padding”有效
2. `k>1.0` 是否对 `eye_occluded` 带来稳定增益

若不能稳定回答上述问题，则不继续放大上下文尺度。

### 1.12.4 Stage T2/T3/T4 配置约束

在完成 T1 后，后续训练统一遵循以下约束：

- 除被比较变量外，其余配置保持不变
- T2 仅改 loss
- T3 仅改输入构造
- T4 才允许组合最优项进行联合验证
- 所有正式实验均绑定：
  - `source_version`
  - `split_version`
  - `input_version`
  - `train_cfg_version`
  - `eval_protocol_version`
  - `preprocess_spec_version`
  - `quant_calib_version`

### 1.12.5 指标

统一关注：

- `Macro-F1`
- `Accuracy@1`
- `Macro-Precision`
- `Macro-Recall`
- 各类别 `Precision / Recall / F1`
- `FP32 / Quant / Drop`

重点关注：

- `eye_narrow` 的 Recall / F1
- `eye_occluded` 的 Recall / F1
- `eye_open` 的 Precision 是否显著下降
- 量化前后 confusion matrix 的迁移方向是否一致

---

## 1.13 Loss 策略约束

### 1.13.1 Weighted CE

历史经验表明：

- 权重过强会导致大类决策边界受损
- 典型现象是 `open -> narrow` 明显增加

因此当前策略为：

- 若重验，加权从温和版本开始
- 采用 `1 / sqrt(freq)` 类形式
- 对权重做 clip 限制范围

说明：

- Weighted CE 只能在 T1 稳定基线上验证
- 不允许在 backbone 未稳定时直接作为主线

### 1.13.2 Focal Loss

历史现象表明：

- `narrow -> closed`
- `open -> narrow`

在 Focal 下恶化明显。

当前判断：

- 当前任务的主要矛盾不太像“hard sample 学得不够”
- 更可能是 `narrow` 本身属于连续边界类，且含有噪声样本或模糊样本
- Focal 强调 hard sample，可能会把边界推得更紧

因此：

- Focal 不作为优先主线
- 若重验，只从小 `gamma` 开始
- 且必须在 V2 稳定基线上验证

### 1.13.3 Label Smoothing

历史经验显示 Label Smoothing 进一步模糊：

- `open`
- `narrow`
- `occluded`

之间的边界。

因此：

- 不作为默认项
- 仅保留为辅助验证项
- 不进入当前正式主线

---

## 1.14 数据版本与实验版本绑定

如果不把数据、split、输入、训练配置、评测口径绑定起来，后续实验很容易失去可比性。

因此每个正式实验必须绑定：

- `source_version`
- `split_version`
- `input_version`
- `manifest_version`
- `train_cfg_version`
- `eval_protocol_version`
- `preprocess_spec_version`
- `quant_calib_version`

要求：

- 历史结果仅作参考，不与新基线直接混算
- 任一关键字段变化，都应形成新版本
- 评测报告中必须打印完整版本信息

---

## 1.15 误分类分析与追溯闭环

### 1.15.1 设计目标

系统需支持从任意一个 ROI 样本或误分类结果，逐级回溯至其对应的：

- ROI 文件
- ROI 导出配置
- 原始图片
- 原始 JSON 标注
- 原始标注框
- 实际裁剪框
- 场景元信息

用于支持：

- 标签错误排查
- 误分类聚类分析
- 输入构造策略分析
- 数据分布偏差分析

### 1.15.2 追溯原则

#### 1.15.2.1 单样本可追溯

``````text
ROI 样本 -> roi_manifest 条目 -> 原始图片 / 原始 JSON / bbox_gt / bbox_used / meta
``````

#### 1.15.2.2 误分类可追溯

测试集中每一个误分类样本，必须能够导出完整追溯信息。

#### 1.15.2.3 结果与版本绑定

避免不同版本之间出现样本路径相同但语义不一致的情况。

### 1.15.3 测试阶段统一输出

``````text
test_trace_all.jsonl
test_trace_misclassified.jsonl
test_trace_misclassified.csv
test_trace_summary.json
``````

每个 trace 条目建议包含：

- `roi_path`
- `img_path`
- `json_path`
- `image_id`
- `group_id`
- `label_gt`
- `label_pred`
- `confidence`
- `topk_probs`
- `split`
- `input_version`
- `train_cfg_version`
- `eval_protocol_version`
- `preprocess_spec_version`
- `bbox_gt`
- `bbox_used`
- `pad_area_ratio`
- `content_area_ratio`
- `bbox_aspect_ratio`
- `context_ratio`
- `meta`

### 1.15.4 混淆对统计

按 `(gt_label, pred_label)` 聚合主要混淆对，例如：

- `eye_open -> eye_narrow`
- `eye_narrow -> eye_closed`
- `eye_occluded -> eye_open`

同时按以下维度进一步聚合：

- `camera`
- `scene`
- `illumination`
- `weather`
- `accessory`
- `pose`
- `seat`
- `group_id`

并新增按输入观测变量聚合：

- `pad_area_ratio`
- `bbox_aspect_ratio`
- `context_ratio`
- `input_version`

目的：

- 判断误分类是否集中于大 padding、高长宽比或特定 crop 模式
- 区分“输入构造问题”和“表征能力问题”

### 1.15.5 标注质量排查规则

#### 1.15.5.1 高置信误分类样本

满足：

- `correct = false`
- `confidence >= 0.9`

优先检查：

- 标签错误
- ROI 导出错误
- 原始框错位
- 输入协议不一致

#### 1.15.5.2 边界类别样本

优先抽检：

- `eye_open -> eye_narrow`
- `eye_narrow -> eye_closed`
- `eye_closed -> eye_narrow`

#### 1.15.5.3 遮挡类异常样本

对于 `eye_occluded` 误分类样本，重点检查：

- ROI 是否覆盖真实遮挡区域
- 是否需要更大上下文
- 标签是否混淆“眯眼”与“遮挡”

---

## 1.16 narrow 与 occluded 的分治分析

这是当前正式方案新增的重点。因为 `eye_narrow` 与 `eye_occluded` 的问题机制不同，不能继续放在同一优化假设下。

### 1.16.1 eye_occluded：上下文专题分析

当前判断：

- `eye_occluded` 更像输入信息不足问题
- 更依赖：
  - 镜框
  - 发丝
  - 手部
  - 阴影
  - 上下眼周上下文

因此重点观察：

- `SquareCrop(k=1.0)` 是否提升 `occluded Recall/F1`
- `k>1.0` 是否继续提升 `occluded Recall/F1`
- `occluded -> open` 是否下降
- 提升是否集中于特定 `accessory / illumination / scene`

建议输出专题报告：

``````text
occluded_context_report.json
``````

建议统计字段：

- `Recall/F1 by input_version`
- `Recall/F1 by context_ratio bucket`
- `occluded -> open` by accessory
- `occluded -> open` by illumination
- `occluded -> open` by pad_area_ratio

### 1.16.2 eye_narrow：边界类专题分析

当前判断：

- `eye_narrow` 不只是普通小类召回不足
- 更可能来自：
  - 类别定义连续
  - 标注边界模糊
  - 中间态天然不稳定

因此重点观察：

- `open -> narrow`
- `narrow -> closed`
- `closed -> narrow`
- 高置信误分类是否集中于 narrow
- 是否集中在特定动作段、模糊帧、边界帧

建议输出专题报告：

``````text
narrow_boundary_report.json
``````

建议统计字段：

- 相邻类别混淆对统计
- 高置信误分类占比
- confidence / margin 分布
- 误分类样本抽检结论
- 是否存在标注标准不一致

---

## 1.17 最终实验主线

正式实验主线按以下顺序推进：

### 1.17.1 Stage T1：MobileNetV2 基线重建

- 固定单一输入基线
- 固定 CE
- 扫 `lr / wd / EMA / 轻量增广`
- 以 Quant 表现优先选 baseline

### 1.17.2 Stage T2：Loss 对照实验

- 只改 loss
- 其余保持不变
- 重点验证：
  - 温和 Weighted CE
  - 小 gamma Focal
  - Label Smoothing 辅助验证

### 1.17.3 Stage T3：输入构造对照实验

- 先验证 `RectROI -> SquareCrop(k=1.0)`
- 再验证 `k=1.4 / 1.8 / 2.2`

### 1.17.4 Stage T4：联合验证

- 仅组合 T2/T3 中最优项
- 评估联合收益是否真实存在
- 若联合后量化掉点扩大，则不进入正式 baseline

---

## 1.18 当前正式结论

在当前约束下，正式方案更新为：

1. split 主键仅使用 `group_id = f(person_id)`  
2. 仅使用 `total_samples >= N_min` 作为 `val/test` 候选门槛  
3. 暂不引入时序约束、覆盖度约束与受约束分配  
4. 正式训练输入主线统一切换到 `const padding`  
5. `RectROI + ConstPad` 作为部署对齐基线  
6. `SquareCrop` 用于拆解“减 padding”和“加上下文”两个效应  
7. MobileNetV2 必须先完成正式 baseline 重建  
8. baseline 选择以 `Quant Macro-F1` 为主导指标  
9. 训练/量化/部署预处理协议必须版本化并写死  
10. ROI 导出必须记录 shortcut 观测字段  
11. 测试阶段必须输出全量 trace 文件  
12. `eye_narrow` 与 `eye_occluded` 必须分别进行专题分析  
13. 所有正式实验必须绑定完整版本信息  
14. 正式评测仅使用 GT 口径  
15. 只有通过阶段 gate 的方案才允许进入下一阶段  