---
status: active
evidence_level: corroborated
evidence_refs:
  - 02_Projects/Knowledge-Base/Current Maintenance Records/知识库治理Skill阶段0至3B实现记录-2026-06-22.md
  - /mnt/d/codex-capability-registry/skills/knowledge-base-structure-builder/SKILL.md
review_after: 2026-07-22
supersedes: []
superseded_by: []
protection_level: normal
change_policy: free_update
---

# 1 knowledge-base-structure-builder 子项目总览

## 1.1 子项目定位

`knowledge-base-structure-builder` 是面向 Markdown 知识库结构化、规范化、写入治理和 indexing-prep 的 authoring skill。它把知识库维护成可稳定召回、可追溯、可执行写前门禁的形态，但不拥有自然语言 Query Planning、历史上下文召回或相关性排序的实现权威。

## 1.2 核心职责

- Bootstrap、Migration、Maintenance 和 current lifecycle。
- 知识、项目、候选、来源和归档的分层放置。
- 入口、overview、current 文档组、链接和 supersession 维护。
- 生命周期与门禁 frontmatter 的 schema、lint 和兼容解释。
- retrieval header 的 authoring 规范、完整性检查和过期检查。
- 项目或子项目级 Fix Registry 的 authoring 规范、完整性检查和重建约束。
- 可重建 metadata、trace/indexing projection 的准备能力。
- 基于目标风险、授权、原文读取证据、change policy 和 hash 的确定性写前门禁。
- 写后 lint、入口同步和结构审计。

## 1.3 明确不负责

- 从自然语言变更意图生成检索词和检索面。
- 同义词、旧名称、中英文别名和问题症状扩展。
- 多轮历史检索、结果排序和召回充分性判断。
- 把没有检索命中解释为不存在历史约束。
- 为弱结构或其他版本知识库提供通用 RAG 能力。
- 将目标仓库 `AGENTS.md` 打包进 skill 或与 skill 版本绑定。

## 1.4 与 retriever 的协作契约

写入前需要历史影响分析时，Structure Builder 消费 Retriever 生成的 `retrieval_package`，但不复制其查询和排序实现。Structure Builder 只验证：

1. 检索是否限定在本次授权范围。
2. 高风险目标是否完成必要 `rg` batch 和原文读取。
3. package 引用的候选来源是否仍可读，并在 Builder 侧按门禁需要最小化计算或校验 hash。
4. 是否存在候选 active constraint、历史修复或 supersession 冲突需要进一步读取和判断。
5. 当前 change policy 应返回 `allow`、`manual_review` 还是 `blocked`。

Retriever 的低置信度、无结果或未解决歧义不得被 Builder 自动解释为安全。

## 1.5 风险分级门禁

| 变更 | 历史检索要求 | 写入门禁 |
|---|---|---|
| 普通 Inbox 新建 | 无或轻量 | 授权和落点检查 |
| normal 文档追加且不改变结论 | 局部 | target hash 和 append 检查 |
| 项目设计或 current 修改 | 完整 | 原文读取、影响分析和必要时人工复核 |
| guarded、verified、critical 修改 | 完整 | 自动化只生成 proposal 或 patch draft |
| supersede、delete、move、promote | 完整 | 关系、证据和双向 supersession fail-closed |

门禁保持确定、窄接口和可审计，不在 gate 内实现检索策略。

## 1.6 文档三层契约

### 1.6.1 Frontmatter

只保存门禁和生命周期字段：`status`、`evidence_level`、`evidence_refs`、`review_after`、`supersedes`、`superseded_by`、`protection_level`、`change_policy`。

### 1.6.2 Retrieval header

正文中的稳定检索锚点可包含 topic、component/symbol、alias/previous name、symptom/failure mode、affected path、constraint 和 related decision。它只增强召回，不替代正文事实，不允许关键词堆砌。

### 1.6.3 正文

保存完整解释、背景、设计或修复过程、适用边界和验证证据。Retrieval header 与正文冲突时，以正文及其可读取证据为准。

## 1.7 AGENTS.md 边界

目标仓库 `AGENTS.md` 是本地政策文件。Skill 只发现、读取并执行它，不携带权威模板，不自动创建或覆盖它，也不要求它与 skill 版本同步。运行报告可记录 policy path 和 hash，但该记录只用于证明本次运行采用了哪份政策。

## 1.8 当前状态与下一步

- 现有 skill 已实现 metadata、lint、Traceability Index、preflight 和 hash check 的首版。
- 当前检索逻辑仍与 gate 耦合，后续应将 Query Planning 和排序权威移出。
- 索引、候选匹配和原文读取必须统一接受 authorized paths；不得先全库扫描再过滤结果。
- 应移除 `assets/AGENTS.knowledge-base.template.md` 及 Bootstrap 中默认创建或 patch `AGENTS.md` 的行为。
- 在 Retriever 交换契约稳定后，Builder 增加 `retrieval_package` 校验和风险分级 preflight。

## 1.9 实施路线

1. 使目录枚举、metadata、索引、匹配、链接扩展和原文读取统一接受 authorized paths。
2. 从现有 CLI 中移除 query term 生成和相关性排序权威，保留 metadata、lint、indexing-prep、gate 和 hash check。
3. 固化最小生命周期 frontmatter、retrieval header、legacy 缺省解释和 lint。
4. 校验 Retriever package 的版本、授权范围、原文 section、歧义和 limitation，并在 Builder 侧按门禁需要最小化计算目标与来源 hash。
5. 实现普通新建、非结论追加、current 修改、protected 修改和 supersession 的分级门禁。
6. 继续 current 去历史化、记录迁移、入口同步和 supersession 完整性检查。

退出条件：Builder 不再生成自然语言检索计划；高风险 gate 输入可追溯到本地政策、目标原文、Retriever 原文读取证据和稳定 rule ID。

## 1.10 验收标准

- 自动化直接修改 verified、guarded、critical 或 active-constraint 文档的次数为 0。
- append-only 历史内容不被自动改写或删除。
- 结论替代具备双向 supersession、原因和可读取证据。
- policy、目标或 Builder 已纳入门禁的来源 hash 变化会使 preflight 失效。
- 删除缓存后能够从授权范围内的 Markdown 重建派生物。
- `AGENTS.md` 不作为 skill 资产，也不由 skill 默认创建或覆盖。
- Builder 不包含 Query Planning、同义词扩展或相关性排序的第二实现。

## 1.11 Fix Registry

Builder 新增 Fix Registry，但它只属于结构化 authoring 和 indexing-prep，不是检索引擎，也不是事实源。它的作用是把历史修复的模块、路径、符号、症状、概要、约束和验证记录集中成一个高召回入口，帮助 Retriever 和写前门禁更快定位原文。

### 1.11.1 粒度

Fix Registry 默认按项目或子项目维护，避免全局索引过大：

- 小项目可使用项目级 registry，例如 `02_Projects/investment-advisor`。
- 大项目中的高频模块应使用子项目级 registry，例如 `02_Projects/DMS/04_Tracking`。
- 顶层全局入口最多只做 registry 发现或路由，不汇总所有 fix 事实。

### 1.11.2 生成与维护

Fix Registry 的主要维护方式是从 fix 原文、retrieval header、`evidence_refs` 和 supersession 关系中生成或校验。人工只能补充检索锚点或修正摘要，不能绕过 `source_fix_doc` 创建事实性条目。

允许的人工维护范围：

- 补充缺失的 alias、症状表达、符号名或路径锚点。
- 修正从原文抽取出的摘要措辞。
- 标记 registry 条目需要重新读取原文或人工复核。

禁止的人工维护范围：

- 创建没有 `source_fix_doc` 的事实性 fix 条目。
- 用 registry 摘要替代原始 fix 记录、验证记录或 supersession 记录。
- 绕过原文、section、evidence_refs 或 supersession 变化直接声明条目仍然有效。

### 1.11.3 字段

每条 registry 记录至少包含：

- `source_fix_doc`：原始修复记录路径。
- `source_section`：原文 section 或 heading。
- `source_fingerprint`：原始修复文档或 section 的轻量指纹，可由路径、section、mtime、size 或调用方已有 hash 组成；默认不要求为每个子字段单独计算 hash。
- `registry_entry_fingerprint`：registry 条目自身的轻量指纹，用于发现人工编辑和生成结果变化；只有门禁、发布或复核需要时才升级为 hash。
- `module`：项目、模块或子系统。
- `affected_paths`：涉及的代码、配置或文档路径。
- `symbols`：函数、类、配置键、CLI、模型名等。
- `symptoms`：问题表现、错误文本、失败模式或用户可见异常。
- `summary`：修复概要。
- `constraints`：后续修改必须保持或避免的约束。
- `validation`：验证命令、证据等级或未闭合验证项。
- `status`：active、superseded、partial、unverified 等。

### 1.11.4 规则

- Fix Registry 是横向索引；每个 fix 原文仍必须有自己的检索锚点和完整正文。
- Registry 命中只能作为候选召回，不能替代读取 `source_fix_doc` 的原文 section。
- Registry 的新增、更新、移动或删除必须由对应原始 fix 记录驱动；不得手写一个没有原文支撑的孤立 registry 条目。
- 原文、section、evidence_refs、supersession 或验证状态变化时，registry 必须被视为过期并重新生成或更新；实现应优先用轻量 fingerprint、mtime/size 或输入侧缓存减少不必要的 hash 计算，只有写前门禁、发布或复核需要强一致证据时才计算 hash。
- Builder 负责 registry 的字段规范、lint、过期检查和写入门禁；Retriever 只把 registry 当作授权范围内的可读召回入口。
